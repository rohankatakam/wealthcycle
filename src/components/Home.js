import React, { useState } from 'react';
import { Grid } from '@mui/material';

const Home = () => {
    return (
        <div>
            <Grid container spacing={2}>
                Wtw
            </Grid>
        </div>
    );
};

export default Home;
